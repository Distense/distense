pragma solidity ^0.4.11;
/*
import './lib/StringArrayUtils.sol';
import './DIDToken.sol';

contract Tasks {
  using StringArrayUtils for string[];

  DIDToken didToken;

//  Distense votable parameters
  uint256 public requiredDIDTaskApprovalThreshold;  // percent of DID/quorum required to approve a proposal to a task
  uint256 public requiredDIDPullRequestApprovalThreshold;  // percent of DID/quorum required to approve pull requests
  uint256 public parameterVotingInterval;  // Period of time between when voters can update these Distense parameters


  function Distense(address _DIDTokenAddress) {
    requiredDIDApprovalThreshold = 40;
    DIDToken didToken = DIDToken(_DIDTokenAddress);
  }

  // Make sure voter hasn't voted and the reward for this task isn't set
  function updateDistenseParameter(string _parameter, uint256 _newValue) external returns (uint256) {
    require(!enoughTimeSinceLastVote(_parameter));

  }

}
*/
